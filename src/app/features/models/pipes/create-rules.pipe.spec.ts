import { CreateRulesPipe } from './create-rules.pipe';

describe('CreateRulesPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateRulesPipe();
    expect(pipe).toBeTruthy();
  });
});
